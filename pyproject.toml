[project]
name = "builder-optimize-7k"
version = "0.1.0"
description = "Constraint-based equipment build optimizer for Seven Knights Rebirth"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["."]
addopts = "-q"
